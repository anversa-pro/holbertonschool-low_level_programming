include "holberton.h"

/**
 * print_buffer - prints a buffer, 10 bytes per line.
 * *@b: pointer to the buffer
 * @size: buffer to print if it is greater then zero.
 * Return: Empty
 */

void print_buffer(char *b, int size)
{
	printf("\n");
}
