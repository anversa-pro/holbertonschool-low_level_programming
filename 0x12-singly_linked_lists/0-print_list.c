#include "lists.h"
/**
 * print_list - Function that prints all the elements of a list_t list.
 * *@h: pointer to list
 * Return: Nodes number
 */

size_t print_list(const list_t *h)
{
	int nodesNumber = 0;

	Return(nodesNumber);
}
