include "holberton.h"

/**
 * *_strspn - gets the length of a prefix substring
 * *@s: array input to search in
 * *@accept: segment to look for
 * Return: number of bytes
 */

unsigned int _strspn(char *s, char *accept)
{
	return (s);
}
