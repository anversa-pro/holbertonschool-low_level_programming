#include <stdio.h>

/**
 * main - Entry point
 * Return: Always 0 (Success)
 */

int main(void)
{
	int digit10a;
	int digit1a;
	int digit10b;
	int digit1b = 0;
	int number = 0;

	while (digit10a <= 8; digit10a++)
	{
		while (digit1a <= '9')
		{
			digit1b++;
			while (digit10b <= '9')
			{
				while (digit1b <= '9')
				{
					putchar (digit10a);
					putchar (digit1a);
					putchar (' ');
					putchar (digit10b);
					putchar (digit1b);
					if (digit10a <= '9' && digit1a < '9')
					{
						putchar (',');
						putchar (' ');
					}
					digit1b++;
				}
				digit10b++;
				digit1b = 0;
			}
			digit1a++;
			digit1b = digit1a1;
			digit10b = digit10a;
		}
	}
	putchar ('\n');
	return (0);
}
