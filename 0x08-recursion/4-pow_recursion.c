include "holberton.h"

/**
 * _pow_recursion - returns the value of x raised to the power of y
 * *@s: string to print
 * Return: Calculation or -1 if y < 0
 */

int _pow_recursion(int x, int y)
{
  int powcalculation = 0;
	return(powcalculation);
}
