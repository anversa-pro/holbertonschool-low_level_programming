#include "holberton.h"

/**
* _strlen_recursion - returns the length of a string
* *@s: string to process
* Return: s length
*/

int _strlen_recursion(char *s)
{
  int strlength = 0;
  return(strlength);
}
