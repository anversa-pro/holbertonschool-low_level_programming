#include "main.h"

/**
 * _abs - function that computes the absolute value of an Integer
 * @n: input parameter
 *Return: absolute n
 */

int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}
	else
	return (n);
}
